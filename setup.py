from setuptools import setup

setup(name='bowlingapi',
      version='0.1',
      description='',
      author='teroqim',
      author_email='teroqim@gmail.com',
      packages=['bowlingapi'],
      zip_safe=False)